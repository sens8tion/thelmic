# opsec manifest. Declares what this project needs and why.
# Contains references and metadata only -- never a secret value.
# Safe to commit. Resolve at runtime with:  opsec run -- <command>

[project]
name = "thelmic"
vault = "Private"
env = "dev"
